node_modules
dist
.DS_Store
*.local
.env
.env.*
_raw-models
*.log
.vscode
.idea
thumbs.db

# Working dropboxes — raw uploads before they get processed/placed
/Extra/
/static/models/wildlife/incoming/

# Verification screenshots — output of .verify-*.mjs harness scripts.
# The scripts themselves are tracked; the shots they produce are not.
/.verify-shots/

# Ad-hoc perf probe — playwright-driven scene measurement, regenerated
# as needed during optimization passes.
/perf-probe.mjs
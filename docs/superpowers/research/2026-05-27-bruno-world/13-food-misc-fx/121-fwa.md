# 121_fwa.py — 6 FWA badges hidden inside explosive crates

**Path:** `folio-2025/scripts/blender_world_steps/steps/121_fwa.py`
**Lines:** 81
**Adds:** 6 objects (6× MESH) to collection `fwa` (lowercase)
**Group:** [13-food-misc-fx](../13-food-misc-fx.md)

## What it does (code-level)
6 nearly-identical objects — `explosiveCrates.005` through `.011` (skipping `.009`) — all wrap mesh `Cube.053`. Each:
- Hand-placed location at `(20-30, 16-22, 0.51)` — clustered near `(24, 21)` in the race-track area.
- `rotation_mode = 'XYZ'`, `rotation_euler` = `(π, 0.042, varying-z)` — flipped upside-down (x=π) with slight y-pitch and per-instance z rotation.
- `scale = (1.0, 1.0, 1.0)` (essentially).
- No modifiers, no custom properties.

One outlier: `explosiveCrates.010` at z=1.626 (others at z=0.512) and with different rotation `(3.179, 0.020, -0.038)` — sits on top of another crate.

## Key data
- **Datablocks referenced**: mesh `Cube.053` (all 6 instances share it).
- **Materials assigned**: `palette` (via mesh data).
- **Modifiers added**: none.
- **Custom properties**: none.
- **World positions**:
  - 5 crates clustered at z=0.512 near `(24, 21)`.
  - 1 crate (`.010`) at z=1.626 — stacked on another.
- **Object types breakdown**: 6 MESH.
- **Parent collection**: `explosiveCrates` (lives at the race-track area).

## Technique / recipe
- **6 instances of one crate mesh**, all flipped upside-down (rot.x ≈ π) — the badges face DOWN inside the crates, visible only after destruction.
- The single elevated crate (`.010`) at z=1.626 is **stacked on top of another** — Bruno is using realistic crate-pile arrangement.
- **No collider EMPTY** here — the runtime probably uses the parent `explosiveCrates` (the destroyable crate objects from script 075-something) for collision, and these `.005-.011` badges only become physical after the crate breaks.
- EXCLUDED in finalize — hidden until the destruction event.

## Connections
- **Reads from**: `005_meshes_*` (`Cube.053`).
- **Read by**: `999_finalize.py` (parents under `explosiveCrates` empty + sets view-layer EXCLUDE).
- **Depends on**: foundation 001-013, parent crate collection.
- **Depended on by**: 999_finalize.

## Notable code patterns
- Hand-tuned per-instance z rotations: `3.227`, `-2.484`, `-2.760`, `-1.107`, `-0.038`, `-3.147` rad — randomized look but exact float values, suggesting Bruno tuned them in Blender by eye rather than via a random seed.
- Naming `explosiveCrates.005..011` (skipping `.009`) — the `.NNN` numbering is from Blender's auto-suffix on duplication; gaps are artifacts of Bruno's authoring history.
- All flat at z=0.512 + 1 stacked at z=1.626 — confirms intentional pile arrangement.

# 130_egg.py — the egg + light-beams reward mesh

**Path:** `folio-2025/scripts/blender_world_steps/steps/130_egg.py`
**Lines:** 33
**Adds:** 2 objects (2× MESH) to collection `egg`
**Group:** [13-food-misc-fx](../13-food-misc-fx.md)

## What it does (code-level)
Tiny script. Adds two meshes:
1. `egg` (MESH `Sphere.005`) at `(0, 0, 2.98)` rot z=1.24 rad — the egg reward at origin (will be moved by parenting). z=2.98 puts it at chest-height.
2. `beams` (MESH `Plane.032`) at `(-0.0166, 0.020, 2.97)` rot `(-1.416, -1.111, 2.108)` — angled light-beam radial plane around the egg.

Both share scale `(1, 1, 1)` and `display_type='TEXTURED'`.

## Key data
- **Datablocks referenced**: meshes `Sphere.005` (egg), `Plane.032` (beams).
- **Materials assigned**: per group index, "—" — but in practice the egg mesh data likely carries `palette` and the beams an emissive material from the radial gradient family.
- **Modifiers added**: none.
- **Custom properties**: none.
- **World positions**: both near origin — authored at origin, parented to a higher zone empty in finalize.
- **Object types breakdown**: 2 MESH.
- **Parent collection**: `egg` (this script), itself under `easter` (script 129) per finalize. Both EXCLUDED.

## Technique / recipe
- **"Reward at origin" pattern**: when a runtime spawnable lives at world origin in the .blend, you know it gets repositioned at spawn-time by the runtime (this `egg` will be picked up at the end of the easter hunt and shown at the player's position or a fixed reveal location).
- The angled rotation of `beams` (`-1.416, -1.111, 2.108` rad) suggests it's been hand-rotated to angle the light beams in a specific dramatic direction — Bruno tuned the reveal animation pose.

## Connections
- **Reads from**: `005_meshes_*` (`Sphere.005`, `Plane.032`).
- **Read by**: `999_finalize.py` (parents under `easter` empty; sets view-layer EXCLUDE on `egg`).
- **Depends on**: foundation 001-013, `129_easter.py` (parent collection registration).
- **Depended on by**: 999_finalize.

## Notable code patterns
- Bruno has 2 different egg-themed collections: `easter` (this one — the reward egg) and `easter.001` ([080](080-easter.001.md), the hunt setup with 6 spawnable eggs). They're related by storyline (find 6 eggs → unlock 1 reward egg) but kept as separate collections for view-layer control.
- Tiny script (33 lines) — Bruno splits each conceptual unit into its own script even when minimal.

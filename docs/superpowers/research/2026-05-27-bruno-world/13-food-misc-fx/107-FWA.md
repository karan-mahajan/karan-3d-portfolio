# 107_FWA.py — FWA award badge (paired with statue, EXCLUDED)

**Path:** `folio-2025/scripts/blender_world_steps/steps/107_FWA.py`
**Lines:** 65
**Adds:** 4 objects (2× MESH, 2× EMPTY) to collection `FWA`
**Group:** [13-food-misc-fx](../13-food-misc-fx.md)

## What it does (code-level)
Tiny script. Creates:
- `refFwaPhysicalDynamic` (MESH `Plane.059`) at `(26.02, 18.05, 2.37)` — uses **quaternion** rotation (1,0,0,0) = identity. The visible FWA plaque mesh.
- `Cube.004` (MESH `Cube.008`) at `(25.43, 17.62, -0.07)` — a base/pedestal at ground level.
- `cuboid.087` (EMPTY `CUBE`, scale 4.22 × 0.88 × 0.53) at `(26.02, 18.05, 2.37)` — large flat collision/trigger box on top of the plaque.
- `cuboid.088` (EMPTY `CUBE`, scale 3.59 × 0.88 × 1.14) at the SAME position — a vertically taller volume.

## Key data
- **Datablocks referenced**: meshes `Plane.059` (FWA plaque face) and `Cube.008` (pedestal base).
- **Materials assigned**: `palette` (via mesh data).
- **Modifiers added**: none.
- **Custom properties**: none.
- **World positions**: all centered at `(26.0, 18.0)` — inside the statue zone where Bruno's statue character holds awards.
- **Object types breakdown**: 2 MESH, 2 EMPTY.
- **Parent collection**: `statue/FWA` per the group index.

## Technique / recipe
- **Quaternion rotation on the mesh** (rare in Bruno's scripts — almost everything else is XYZ Euler). The identity quaternion (1,0,0,0) suggests this is a placeholder Bruno authored in quat mode for a specific reason (perhaps to avoid gimbal lock during a runtime "spin" animation when the award appears).
- **Two stacked cuboid volumes at the same position with different scales** — one (4.22×0.88×0.53) is wide/flat, the other (3.59×0.88×1.14) is taller. Likely outer "approach" trigger + inner "pickup" volume.
- EXCLUDED in finalize — only visible after the statue interaction grants the award.

## Connections
- **Reads from**: `005_meshes_*` (`Plane.059`, `Cube.008`), `004_materials.py` (palette).
- **Read by**: `999_finalize.py` (parents under `statue` and sets view-layer EXCLUDE).
- **Depends on**: foundation 001-013.
- **Depended on by**: 999_finalize.

## Notable code patterns
- `rotation_mode = 'QUATERNION'` — unique among most Bruno scripts. Worth noting if we ever replicate this prop, the runtime probably reads `rotation_quaternion` not euler.
- Tiny script (65 lines) — Bruno has many micro-scripts for individual props rather than batching. Per-collection isolation = idempotent re-running.
- Compare to [121_fwa.py](121-fwa.md) (the lowercase `fwa` collection) — that one is the badges revealed when explosive crates are destroyed; this one (uppercase `FWA`) is parented to the statue.

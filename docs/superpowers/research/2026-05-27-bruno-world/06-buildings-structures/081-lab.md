# 081_lab.py — lab zone scaffold with carpet, fire anchor, and wall physics

**Path:** `folio-2025/scripts/blender_world_steps/steps/081_lab.py`
**Lines:** 173
**Adds:** 11 objects to collection `lab`
**Group:** [06-buildings-structures](../06-buildings-structures.md)

## What it does (code-level)

Creates collection `lab`. Adds 11 objects:

**Zone anchor empties** (8 EMPTY):
- `refInteractivePoint.001` — PLAIN_AXES, at (13.67, -18.61, 1.50), rotation Z=0.286 rad — player approach direction
- `physicalFixed` — PLAIN_AXES, at (13.19, -16.66, 1.00) — physics anchor
- `cuboid.069` — CUBE display (0.5 size), at (-28.42, 22.05, 1.97), scale=(0.54, 4.71, 3.98) → wall collision 9.42m × 7.97m
- `cuboid.071` — CUBE display, at (-25.10, 24.34, 2.50), scale=(0.54, 2.71, 4.90) → wall collision
- `refZoneBounding.004` — CIRCLE, at (13.14, -17.68, 3.27), scale=7.219 → r=7.22m
- `refFire` — PLAIN_AXES 0.5 size, at (11.14, -17.30, 0.37) — VFX fire spawn point
- `lab` — PLAIN_AXES root empty, at (13.14, -17.68, 3.27) (co-located with bounding)
- `refZoneFrustum.004` — CIRCLE, at (12.10, -14.70, 3.27), scale=5.798 → r=5.80m

**Visible geometry** (3 MESH):
- `refCarpet.001` — mesh `Plane.028`, at (13.69, -18.65, 0.019), rotation Z=0.262 rad, scale≈1.0 — the lab carpet
- `refWood` — mesh `Cube.095`, at (11.11, -17.57, 0.047), rotation (0.407, 0.429, 3.117) rad — a fallen/tilted log or wooden prop
- `Cube.003` — mesh `Cube.003`, at (10.77, -15.34, 1.82), rotation Z=0.698 rad — a structural cube

Lab center: **(13.14, -17.68)**. Northwest area of island.

## Key data

- **Datablocks referenced:** `Plane.028` (carpet), `Cube.095` (wood), `Cube.003`
- **Materials assigned:** `labCarpet` (on refCarpet.001), `palette` (others) — via mesh datablocks
- **Modifiers added:** none
- **World positions:** lab center ~(13, -18). Zone bounding r=7.22m.
- **Object types breakdown:** 3 MESH, 8 EMPTY
- **Child collections (via finalize):** `mainTable`, `scroller`, `board.001`, `blackBoard.001`, `cauldron`, `sideTable`

## Technique / recipe

Lab uses the standard Bruno zone pattern + three additional features: a fire VFX anchor (`refFire`), lab carpet decal (`refCarpet.001`), and two wall-collision CUBE empties for the lab room walls. The CUBE empties (`cuboid.069`, `cuboid.071`) are positioned far at (-28, 22) — same "local template" positioning pattern seen in `timeMachine`; the runtime reads them relative to the lab root.

The `refWood` mesh has large X and Y rotations (0.407, 0.429 rad ≈ 23°, 25°) suggesting it's an overturned log lying at an angle — adds visual chaos to the lab floor.

## Connections

- **Reads from:** `005_meshes_*.py`, `004_materials.py` (`labCarpet`, `palette`)
- **Read by:** `999_finalize.py` (parents lab under `areas`; 6 child collections parented under lab)
- **Depends on:** `013_collections.py`, `044_areas.py` (not in this batch; creates `areas` container)
- **Depended on by:** `082_mainTable.py`, `083_scroller.py`, `085_board.001.py`, `086_blackBoard.001.py`, `087_cauldron.py`, `090_sideTable.py` (not in this batch)

## Notable code patterns

`refFire` EMPTY with display_size=0.5 (small) — the VFX fire in Bruno's runtime is spawned at this position. The CUBE-type empties for wall collision differ from the MESH-based cuboid colliders in the altar (048) — Bruno uses EMPTY/CUBE for simple wall boxes and MESH/WIRE for more complex collision shapes.
